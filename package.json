{
  "name": "wtfiwant",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --env-file=../../.env --cwd apps/web dev",
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome check --write .",
    "db:migrate": "bun run --cwd packages/api db:migrate",
    "commerce:reconcile": "bun run --cwd packages/api commerce:reconcile"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.2",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
